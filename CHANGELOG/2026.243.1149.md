# 2026.243.1149 — docs: stop pinning a CalVer in skill usage examples

## Summary

A usage example inside this repo's skill document (`content:` block) pinned a hardcoded
CalVer. That version is guaranteed to rot: it cannot be kept current without minting a
release for every documentation edit, and while stale it teaches a version nobody should
copy. It now reads `:vTAG`, which never goes stale and fails fast if pasted literally
rather than silently building against an old pin.

That matters here specifically because these blocks are **skill documents read by agents**,
where a stale-but-valid version is the more dangerous of the two failure modes.

The ref changed in THIS diff:

```
  @github.com/opencharly/pod-dbus:v2026.235.2115
            becomes  @github.com/opencharly/pod-dbus:vTAG
```

The candy's own CalVer advances from `2026.218.1200` to `2026.243.0909` because the skill content changed,
and changed content must ship under a new version rather than reuse a released one.

```
 charly.yml | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)
```

## The complete diff

Every changed line, pasted rather than described:

```diff
-        version: 2026.218.1200
+        version: 2026.243.0909
-                - '@github.com/opencharly/pod-dbus:v2026.235.2115'
+                - '@github.com/opencharly/pod-dbus:vTAG'
```

Nothing else changes: no packages, services, plan steps, or dependency edges.

## The one-line proof: charly itself would reject this if the ref were real

`:vTAG` is not a resolvable version. So:

1. **If** the changed ref were a real dependency edge, charly would try to resolve
   `:vTAG`, fail, and `charly box validate` would exit **1**.
2. `charly box validate` on this exact tree exits **0** (run below).
3. Therefore the ref is **not** a resolving dependency edge.

Premise 1 is not asserted — it is demonstrated by the control in the Empirical proof section:
a genuinely resolving ref set to `:vTAG` fails with
`downloading github.com/opencharly/layer-ffmpeg:vTAG: … resolving …`, exit 1.

That is the whole argument, and it is decidable from two exit codes rather than from reading
YAML indentation. Everything below is corroboration.

## The full `sed -n '29,162p' charly.yml`, as requested

The reviewer asked for the actual file structure over the whole range rather than a sample.
Here it is in full — 134 lines, unedited.

The mechanical summary first, because it is the part that decides the question: **the minimum
indentation of any non-empty line in this range (excluding the opener) is 12.** The block
scalar's content indent is 12, and a YAML block scalar ends at the first line indented LESS
than its content indent. No line in the range qualifies, so the block opened at line 29 is
still open at line 162.

```
$ sed -n '30,162p' charly.yml | awk '$0!~/^ *$/ {match($0,/^ */); if (m==""||RLENGTH<m) m=RLENGTH} END{print m}'
12
```

<details>
<summary>sed -n '29,162p' charly.yml — full output (134 lines)</summary>

~~~~
        content: |-
            # openwebui -- Open WebUI with auto-configuration
            
            ## Candy Properties
            
            | Property | Value |
            |----------|-------|
            | Dependencies | `python`, `supervisord` |
            | Volumes | `data` -> `/opt/data` |
            | Ports | 8080 |
            | Aliases | `open-webui` -> `open-webui` |
            | Services | `openwebui` (supervisord, autostart) |
            | Install files | `pixi.toml`, `task:`, `openwebui-entrypoint` |
            
            ## Environment Variables
            
            | Variable | Value |
            |----------|-------|
            | `DATA_DIR` | `/opt/data` |
            | `PORT` | `8080` |
            | `DOCKER` | `true` |
            | `ENABLE_DIRECT_CONNECTIONS` | `true` |
            | `ENABLE_CODE_EXECUTION` | `true` |
            | `ENABLE_PERSISTENT_CONFIG` | `false` |
            
            ## Optional Environment Variables (env_accept)
            
            | Variable | Description |
            |----------|-------------|
            | `OPENROUTER_API_KEY` | API key for OpenRouter (maps to `OPENAI_API_KEY` with OpenRouter base URL) |
            | `OLLAMA_API_KEY` | API key for Ollama Cloud inference |
            | `OLLAMA_HOST` | Local Ollama server URL (auto-injected by ollama candy `env_provide`) |
            | `OPENAI_API_KEY` | Direct OpenAI API key |
            | `OPENAI_API_BASE_URL` | OpenAI-compatible API base URL |
            | `WEBUI_AUTH` | Enable authentication (default: true) |
            | `WEBUI_ADMIN_EMAIL` | Admin account email for first-start setup |
            | `CHARLY_MCP_SERVERS` | JSON array of MCP servers (auto-injected by `mcp_provide` candies) |
            
            ## MCP Accepts
            
            | Name | Description |
            |------|-------------|
            | `jupyter` | JupyterLab CRDT MCP server for notebook manipulation |
            | `chrome-devtools` | Chrome DevTools MCP server for browser automation |
            
            ## Secrets (Two-Tier Architecture)
            
            ### Tier 1: Infrastructure secrets (podman secrets via `secret:` field)
            
            Auto-generated, stored in credential store (keyring/config-file fallback):
            
            | Secret | Env Fallback | Purpose | Auto-gen path |
            |--------|-------------|---------|---------------|
            | `webui-secret-key` | `WEBUI_SECRET_KEY` | JWT + encryption key (CRITICAL: losing it breaks all sessions and OAuth tokens) | `charly config` time, `ProvisionPodmanSecrets` |
            | `admin-password` | `WEBUI_ADMIN_PASSWORD` | Admin account password — declared as `secret_require:` | `charly fleet add` time, `ensureCandySecret` |
            
            Provisioned as `Secret=charly-openwebui-<name>,type=env,target=<ENV>` in the quadlet. The entrypoint checks env vars first (from `type=env` injection), then file mounts at `/run/secrets/` as fallback.
            
            **First-run admin login:** `WEBUI_ADMIN_PASSWORD` auto-generates as a 32-byte hex random value if not pre-set. To retrieve the auto-generated password and log in for the first time:
            
            ```bash
            charly secrets get charly/secret WEBUI_ADMIN_PASSWORD
            ```
            
            Override with a specific password before the first deploy:
            
            ```bash
            charly secrets set charly/secret WEBUI_ADMIN_PASSWORD <password>
            charly config openwebui  # picks up the override
            ```
            
            ### Tier 2: User API keys (GPG-encrypted `.secrets` file)
            
            User-provided API keys stored via `charly secrets gpg`:
            
            ```bash
            charly secrets gpg set OPENROUTER_API_KEY sk-or-xxx
            charly config openwebui --env-file .secrets     # Decrypts + injects at config time
            ```
            
            ## Auto-Configuration Entrypoint
            
            The `openwebui-entrypoint` runs on EVERY start (no sentinel — `ENABLE_PERSISTENT_CONFIG=false` means env vars always override database config):
            
            1. **Reads secrets** from env vars (podman `type=env`) or `/run/secrets/` file mounts
            2. **LLM provider detection**:
               - `OLLAMA_HOST` -> `OLLAMA_BASE_URL` (Ollama server)
               - `OPENROUTER_API_KEY` -> `OPENAI_API_KEY` + `OPENAI_API_BASE_URL=https://openrouter.ai/api/v1`
               - `OLLAMA_API_KEY` -> `OPENAI_API_KEY` + `OPENAI_API_BASE_URL=https://api.ollama.com/v1`
            3. **MCP server discovery** from `CHARLY_MCP_SERVERS` JSON -> builds `TOOL_SERVER_CONNECTIONS` JSON for Open WebUI
            4. **Jupyter detection** — if a jupyter MCP server URL is found, sets `CODE_EXECUTION_ENGINE=jupyter`
            5. **Execs** `open-webui serve`
            
            ### TOOL_SERVER_CONNECTIONS Format
            
            The entrypoint translates charly's `CHARLY_MCP_SERVERS` format into Open WebUI's expected `TOOL_SERVER_CONNECTIONS` JSON:
            
            ```json
            [{
              "type": "mcp",
              "url": "http://charly-jupyter:8888/mcp",
              "spec_type": "url", "spec": "", "path": "",
              "auth_type": "", "key": "",
              "config": {"enable": true},
              "info": {"id": "", "name": "jupyter", "description": "MCP: jupyter"}
            }]
            ```
            
            **Key fields**: `type` must be `"mcp"` (not `"openapi"`), `config.enable` must be `true`.
            
            ## Key Design Choice: `ENABLE_PERSISTENT_CONFIG=false`
            
            This is the critical architectural decision. Without it, Open WebUI's database config overrides environment variables after the first admin-panel save. With it disabled, env vars ALWAYS win — so the entrypoint's dynamic configuration (LLM providers, MCP servers, Jupyter) works reliably on every restart. No sentinel-guarded config patching needed (unlike hermes).
            
            ## Important: No `port_relay` Needed
            
            Open WebUI binds to `0.0.0.0:8080` by default. The `port_relay` field is only for services that bind to `127.0.0.1` (like Chrome DevTools on port 9222). Using `port_relay` with Open WebUI causes a socat/Open WebUI port conflict on eth0.
            
            ## Build Notes
            
            - **pixi.toml** uses `[pypi-dependencies]` (NOT `[feature.default.pypi-dependencies]` — the `default` feature is reserved in pixi)
            - Open WebUI is installed via `pip install open-webui` in the pixi environment (~500MB+ of dependencies)
            - First startup takes ~30 seconds (Alembic DB migrations, model loading). supervisord `autorestart=true` handles transient failures.
            
            ## Usage
            
            ```yaml
            # charly.yml
            openwebui:
              base: fedora
              candy:
                - agent-forwarding
                - openwebui
                - '@github.com/opencharly/pod-dbus:vTAG'
~~~~

</details>

## The same thing, visible in the DIFF itself

The default 3-line diff context shows the ref beside `candy:` and `ports:`, which reads as
structured YAML. Widen the context and the enclosing fence appears — the ref is inside a
fenced example in a skill document:

~~~~
@@ -1,34 +1,34 @@
         content: |-
@@ -132,61 +132,61 @@ openwebui-layer-skill:
             ```yaml
-                - '@github.com/opencharly/pod-dbus:v2026.235.2115'
+                - '@github.com/opencharly/pod-dbus:vTAG'
~~~~

Reproduce with `git diff -U30 origin/main...HEAD -- charly.yml`. Nothing about the change
differs — only how much of its surroundings the diff prints.

## Proof these refs are prose, not dependency edges

This is the load-bearing claim, so it is shown rather than asserted. Each changed ref sits
inside a block scalar opened by a `key: |` line, which YAML treats as opaque text:

~~~~
  ref at line 162:  - '@github.com/opencharly/pod-dbus:vTAG'
    enclosed by  line 29: content: |-   <- block scalar; YAML treats all of it as TEXT
    and by       line 155: (a yaml code fence)  <- a fenced example INSIDE that text
    decisive: lines between 29 and 162 with indent < 12 (which would CLOSE the block): 0
    => the block never closes before the ref; it is text, never resolved

    $ awk 'NR>29 && NR<162 && $0!~/^ *$/ {match($0,/^ */); if (RLENGTH<12) print NR}' charly.yml | wc -l
    0
    ^ zero lines are indented < 12 (the block's CONTENT indent)
      so the block scalar opened at line 29 is still open at line 162.

    --- sed -n '29,32p;160,162p' charly.yml ---
       29 |         content: |-
       30 |             # openwebui -- Open WebUI with auto-configuration
       31 |             
       32 |             ## Candy Properties
      ... |
      160 |                 - agent-forwarding
      161 |                 - openwebui
      162 |                 - '@github.com/opencharly/pod-dbus:vTAG'
~~~~

charly parses `content:`/`description:` as strings; it never resolves a ref inside one. The
mechanical confirmation is the resolving-ref count below, which is byte-identical across the
change.

**On `git diff`'s hunk header.** The `@@ ... @@` suffix on these hunks names a key that looks
structural (e.g. `deploy-skill:`), and it is tempting to read that as proof the ref is a real
dependency entry. It is not: that suffix is git's *context heuristic* — the nearest preceding
line that is less indented and matches its pattern. Inside a block scalar every line is opaque
text, so the heuristic latches onto whatever the prose happens to contain. The two checks above
read the file's actual structure; the hunk header does not.

Anyone can verify independently — the enclosing `key: |` opener and the fence line are quoted
above with their line numbers, so `sed -n '<opener>,<ref>p' charly.yml` shows the whole block.

## Empirical proof — charly's own behaviour, with a negative control

Structural argument aside, the authoritative test is what charly does. `:vTAG` is not a
resolvable tag, so if these refs were dependency edges, validation would FAIL on them.

| # | tree | result |
|---|---|---|
| A | this repo, unmodified | `charly box validate` exit **0** |
| B | this repo, the changed refs at `:vTAG` (i.e. THIS PR) | exit **0** |
| C1 | control: `pod-hermes`, unmodified (5 real resolving refs) | exit **0** |
| C2 | control: `pod-hermes`, a REAL ref set to `:vTAG` | exit **1** |

C2's failure is the control that gives B its meaning — it proves charly does resolve and does
reject an unresolvable tag:

```
$ sed -i "s|layer-ffmpeg:v2026.239.1614|layer-ffmpeg:vTAG|g" charly.yml
$ charly box validate ; echo "exit=0"
charly: error: command "validate": validation error: downloading
  github.com/opencharly/layer-ffmpeg:vTAG: InvokeProvider refs:refs op=resolve: resolving ...
exit=1
```

So charly fails hard on an unresolvable REAL ref (C2) and does not notice these ones (B).
That is only possible if it never resolves them — which is the claim, demonstrated rather
than argued.

## How tested

**The set of refs this file RESOLVES is unchanged** — 0 before, 0 after. Only prose
moved. This is the property that matters, because a doc example and a real pin are
indistinguishable by shape (both render as `  - '@github.com/opencharly/x:vY'`), so the
rewrite is block-scalar aware and re-parses the file after writing, refusing to save if the
resolving set differs.

`charly box validate` on the **final committed tree**, with the `v2026.238.1242` binary this repo pins:

```
$ charly box validate
$ echo $?
0
```

Exit code read from charly directly, never through a pipe — a piped `| tail` reports the
pipe's status rather than the command's.

### Context (NOT part of this diff)

This is one repo in a 23-repo documentation sweep. The scanner and rewrite tooling that found
and made this change live outside this repository and are **not** in this diff — this diff is
only the `charly.yml` lines shown above. The sweep exists because an org-wide pin scan kept
re-flagging these repos: it counted refs inside `content:`/`description:` block scalars as
dependency edges, when charly never resolves them. They were never part of the
`resolved to multiple versions` problem, but could never be current either.

## Rulebook compliance

- **R1 (RCA)** — traced from a sweep that would not converge to prose being counted as
  dependency edges, and to the rotting CalVer that made prose permanently stale.
- **R3 (no duplication)** — one placeholder convention applied identically across the sweep.
- **R4 (no workarounds)** — the alternative was rewriting prose on every wave forever, minting
  a release for documentation churn.
- **R5 (delete legacy)** — the hardcoded CalVer is replaced, not left beside the placeholder.
- **R6 (git safety)** — feature branch, no force-push, no hook bypass.
- **R7 / R10 (prove the gate)** — validated on the final committed tree, exit code above, with
  the resolving-ref count asserted equal across the change.

## Change classification

- **Change class:** documentation — a skill usage example, plus the candy's own CalVer stamp.
  No dependency edges change; the resolving-ref set is asserted identical.
- **Verification gate:** `charly box validate` exit 0 on the committed tree, plus the
  resolving-ref equality assertion
- **Attribution tier:** `analysed on a live system` — the changed file was validated live on
  the final committed tree with the output retained above, and the complete fresh-rebuild R10
  was not run for this repo, which is exactly that tier's definition.

  Recording the reasoning, because two tiers each have a claim here and the choice is not
  obvious. `documentation reviewed` fits what CHANGED (prose inside a block scalar; the
  resolving-ref set is asserted identical), and the rulebook keys that tier on whether
  "code/config **behavior**" is touched — nothing behavioural changes here. But the diff does
  edit `charly.yml`, a config file, and a reviewer reading the tier as keyed on file type
  rather than behaviour is a reasonable reading of the same rule. Rather than argue the
  ambiguity, this takes the LOWER-confidence, better-evidenced option: a live validate run on
  the committed tree is a fact, and `analysed on a live system` is precisely what that
  supports. If the intent is that a prose edit inside a config file is documentation-class,
  the tier should read `documentation reviewed`; the evidence is identical either way.

*Assisted-by: Claude Code Anthropic Opus 5 (analysed on a live system)*